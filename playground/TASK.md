# Task

Update `src/calculator.ts` so `sumNumbers()` returns the sum of all finite numbers in the input array.

Requirements:

- Ignore `NaN`, `Infinity`, and `-Infinity`.
- Return `0` for an empty array or when no finite numbers exist.
- Keep the function signature unchanged.

After the change, explain which file was modified and why.
