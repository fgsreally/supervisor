# Design

读取 proposal.md、已有项目设计和代码结构。确定技术大方向、包边界、数据归属、主要组件、外部集成、错误策略和代码规范，不进入具体接口字段实现。

遇到多个合理架构方向、兼容性取舍、部署约束或技术栈选择时主动使用 ask。可以委派只读子 Agent 调查现状，但主 Session 负责综合结论和询问用户。

产出 design.md，明确采用方案、放弃方案及原因、结构约束和后续 Spec 必须遵守的边界。
