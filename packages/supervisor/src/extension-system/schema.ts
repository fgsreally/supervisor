/**
 * Re-export TypeBox helpers for extension authors.
 * Prefer: `import { Type, defineExtension } from "@earendil-works/pi-supervisor"`
 */
export { Type, type Static, type TSchema } from "typebox";
