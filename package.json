{
  "name": "supervisor-standalone",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "extensions/*"
  ],
  "type": "module",
  "scripts": {
    "clean": "pnpm --filter \"./packages/*\" --if-present run clean",
    "build": "pnpm --filter pi-supervisor run build",
    "build:ui": "pnpm --filter pi-supervisor-ui run build",
    "build:all": "pnpm run build:ui && pnpm run build",
    "dev": "pnpm --filter pi-supervisor-ui run dev",
    "dev:supervisor": "pnpm run build && pnpm --filter pi-supervisor run serve:playground",
    "dev:seed": "node packages/supervisor-web-ui/scripts/seed-dev-showcase.mjs playground/.supervisor/supervisor.db",
    "playground": "pnpm run build && pnpm --filter pi-supervisor run serve:playground",
    "serve:tunnel": "pnpm run build:all && bun packages/supervisor/dist/cli.mjs serve --port 3030 --cwd playground --password 123456 --tunnel",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "typecheck": "pnpm --filter \"./packages/*\" --if-present run typecheck",
    "format": "oxfmt .",
    "format:check": "oxfmt --check .",
    "check": "pnpm run lint && pnpm run typecheck && pnpm run format:check",
    "test": "pnpm --filter \"./packages/*\" --if-present run test",
    "serve": "bun packages/supervisor/dist/cli.mjs serve --port 3030 --cwd playground --password 123456",
    "docs:dev": "vitepress dev docs",
    "docs:build": "vitepress build docs",
    "docs:preview": "vitepress preview docs"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "oxfmt": "latest",
    "oxlint": "latest",
    "shx": "^0.4.0",
    "tsdown": "^0.22.3",
    "typescript": "^5.9.2",
    "vitepress": "^1.6.4"
  },
  "engines": {
    "bun": ">=1.2.0",
    "node": ">=20.6.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ],
    "ignoredOptionalDependencies": [
      "@anthropic-ai/claude-agent-sdk-darwin-arm64",
      "@anthropic-ai/claude-agent-sdk-darwin-x64",
      "@anthropic-ai/claude-agent-sdk-linux-arm64",
      "@anthropic-ai/claude-agent-sdk-linux-arm64-musl",
      "@anthropic-ai/claude-agent-sdk-linux-x64",
      "@anthropic-ai/claude-agent-sdk-linux-x64-musl",
      "@anthropic-ai/claude-agent-sdk-win32-arm64",
      "@anthropic-ai/claude-agent-sdk-win32-x64"
    ]
  }
}
