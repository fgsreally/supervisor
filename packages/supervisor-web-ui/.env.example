# Supervisor HTTP API base. Leave empty in dev — Vite proxies to localhost:3030.
# VITE_API_BASE=http://localhost:3030

# Workspace directory for sessions, resources, and agent file tools.
# Dev server injects the repo playground path automatically; override here if needed.
# VITE_WORKSPACE_CWD=D:/my-project/supervisor-standalone/playground
