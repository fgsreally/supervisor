\n\n===== UPDATE @ L1781 =====\n@@
-              class="text-[13px] font-medium truncate session-name"
+              class="text-[13px] md:text-[12px] font-medium truncate session-name"
@@
-          <span class="text-[10px] shrink-0 session-time">{{
+          <span class="text-[10px] md:text-[9px] shrink-0 session-time">{{
@@
-          <span class="text-[11px] truncate session-preview">{{ preview }}</span>
+          <span class="text-[11px] md:text-[10px] truncate session-preview">{{ preview }}</span>
\n\n===== UPDATE @ L1859 =====\n@@
-              class="text-[13px] md:text-[12px] font-medium truncate session-name"
+              class="text-[0.8125rem] md:text-[0.923rem] font-medium truncate session-name"
@@
-          <span class="text-[10px] md:text-[9px] shrink-0 session-time">{{
+          <span class="text-[0.625rem] md:text-[0.692rem] shrink-0 session-time">{{
@@
-          <span class="text-[11px] md:text-[10px] truncate session-preview">{{ preview }}</span>
+          <span class="text-[0.6875rem] md:text-[0.769rem] truncate session-preview">{{ preview }}</span>
\n\n===== UPDATE @ L2193 =====\n@@
 .session-time {
   color: var(--app-text-muted);
 }
+
+@media (max-width: 767px) {
+  .session-row {
+    min-height: 4.5rem;
+    padding: 0.75rem var(--m-page-inline) !important;
+    background: var(--m-page-bg);
+  }
+
+  .session-row::after {
+    position: absolute;
+    right: 0;
+    bottom: 0;
+    left: calc(var(--m-page-inline) + 3rem + 0.75rem);
+    height: 1px;
+    background: var(--m-divider);
+    content: "";
+    transform: scaleY(0.5);
+    transform-origin: bottom;
+  }
+
+  .session-row:hover:not(.session-row--active):not(.session-row--achieved),
+  .session-row--active,
+  .session-row--active:hover {
+    color: var(--m-text-primary);
+    background: var(--m-pressed);
+    box-shadow: none;
+  }
+
+  .session-row--active .session-name {
+    color: var(--m-text-primary);
+  }
+
+  .session-row--active .session-preview,
+  .session-row--active .session-time {
+    color: var(--m-text-secondary);
+  }
+
+  .session-row :deep(.session-avatar) {
+    width: 3rem !important;
+    height: 3rem !important;
+    border-radius: 0.375rem !important;
+    font-size: 1rem !important;
+    box-shadow: none;
+  }
+
+  .session-row > div:last-child {
+    gap: 0.75rem;
+  }
+
+  .session-name {
+    color: var(--m-text-primary);
+    font-size: 1rem !important;
+    font-weight: 400;
+    line-height: 1.35;
+  }
+
+  .session-preview {
+    color: var(--m-text-secondary);
+    font-size: 0.8125rem !important;
+    line-height: 1.35;
+  }
+
+  .session-time {
+    color: var(--m-text-muted);
+    font-size: 0.75rem !important;
+    line-height: 1.35;
+  }
+
+  .session-status-ring {
+    width: 0.625rem;
+    height: 0.625rem;
+    border-width: 1px;
+  }
+}
 </style>
